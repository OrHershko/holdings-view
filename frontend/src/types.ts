export interface User {
    id: string;
    name: string;
    avatarUrl: string;
    accountType: string;
  }

  export interface NavItem {
    id: string;
    name: string;
    icon: string;
    path: string;
  }